{
  "name": "reql-helper",
  "version": "1.0.0",
  "description": "* A simple light-weight helper for RethinkDB that utilizes and manages pool connections  \r ## Example Usage",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --listEmittedFiles -p ."
  },
  "author": "SeqSEE",
  "license": "MIT",
  "dependencies": {
    "rethinkdb": "^2.4.2",
    "rethinkdb-pool": "^1.2.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.22"
  }
}
